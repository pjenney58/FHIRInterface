version: '3.8'
# cell IP: 172.58.229.225
# public IP: 38.7.155.219
# LAN IP:  192.168.86.241 
services:    
    kafka:
        image: 'bitnami/kafka:latest'
        networks:
        - backend-network
        environment:
        - KAFKA_CFG_NODE_ID=0
        - KAFKA_CFG_PROCESS_ROLES=controller,broker
        - KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093
        - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
        - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=0@kafka:9093
        - KAFKA_CFG_CONTROLLER_LISTENER_NAMES=CONTROLLER
#        - KAFKA_KRAFT_CLUSTER_ID=PalisaidCluster

  #  postgres:
  #  image: postgres:15.5-bullseye
  #      image: pjenney58/palisaiddb:latest
  #      container_name: db-postgres-all
  #      restart: always
  #      ports:
  #      - 5432:5432

  #      deploy:
  #          endpoint_mode: vip
  #      labels:
  #          com.example.description: "This label will appear on the service"

  #      volumes:
  #      - data:/var/lib/postgresql/data
  #      - ~/.postgres-data:/mnt/configuration-data
  #      - ~/.postgres-data:/root/.gnupg
  #      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
  
  #      environment:
  #          POSTGRES_USER: postgres
   #         POSTGRES_PASSWORD: pgpassword
  
   #     networks:
   #     - backend-network
 
    pgadmin:
        container_name: pgadmin4_container
        image: dpage/pgadmin4
        restart: always
  #      depends_on:
  #      - postgres
        environment:
            PGADMIN_DEFAULT_EMAIL: pete.jenney@gmail.com
            PGADMIN_DEFAULT_PASSWORD: postgres
        ports:
        - "5050:80"
        networks:
        - backend-network

    GatewayController:
        container_name: gatewayapi
        environment:
            - DOTNET_RUNNING_IN_DOCKER_CONTAINER=true
        image: pjenney/gatewayapi
        restart: always
        ports: 
        - "5080:80"
        - "5081:443"
        build:
            context: .
            dockerfile: src/PrimaryControllers/Dockerfile
        networks:
        - backend-network

    CollectorController:
        container_name: collectorapi
        environment:
            - DOTNET_RUNNING_IN_DOCKER_CONTAINER=true
        image: collectorapi
        restart: always
        ports: 
        - "5001:80"
        build:
            context: .
            dockerfile: src/Collectors/CollectorsController/Dockerfile
        networks:
        - backend-network

    Fhir4Collector:
        container_name: fhir4Collector
        environment:
        - DOTNET_RUNNING_IN_DOCKER_CONTAINER=true
    
        image: pjenney/fhir4collector
        restart: always
        ports: 
        - "5002:80"
        build:
            context: .
            dockerfile: src/Collectors/Fhir4Collector/Dockerfile
        networks:
        - backend-network


#    web:
#        image: nginx
#        volumes:
#            - ./templates:/etc/nginx/templates
#        ports:
#            - "80:80"
#            - "443:443" 
#        environment:
#        - NGINX_HOST=registry.sanddriftsoft.com
#        - NGINX_PORT=80
#        networks:
#        - backend-network

networks:
  backend-network:
    driver: bridge

volumes:
  data: